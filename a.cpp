/*
  author : Anand
*/

#include <bits/stdc++.h>

using namespace std;

#define ll long long

void solve(int tc = 1) {
  int x, y;
  cin >> x;
  cout << 2 * x << endl;
}

signed main() {
  cin.tie(nullptr);
  ios::sync_with_stdio(false);

  int tc = 1;
  // cin >> tc;
  for (int t = 1; t <= tc; ++t) solve(t);

  return 0;
}